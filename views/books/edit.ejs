<h2>Actualizar Libro</h2>
<!-- Creating form inside authors/new route & redirecting results to router '/' POST -->
<form action="/books/<%= book.id %>?_method=PUT" method="POST"> <!-- methodOverride, since it is an actual PUT -->
<%- include('_form_fields.ejs') %>  
    <a href="/books">Cancelar</a> <!-- Anchor reference (no btn) as cancel btn just redirect, so does not interact with an objetc -->
    <button type="submit">Actualizar</button> <!-- send back to authors '/' route once completed (hook of the POST route) & render new data over that route -->
</form>